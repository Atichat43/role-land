version: '3.1'
services:
  postgres:
    image: postgres
    environment:
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      # TODO: remove -2
      POSTGRES_DB: test-2
    ports:
      - '5432:5432'
